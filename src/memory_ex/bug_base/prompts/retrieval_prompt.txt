当前任务上下文：
{task_context}

即将修改的文件：
{file_paths}

以下是候选历史Bug记录：
{candidates_json}

请判断每条记录与当前任务的相关性，返回相关的记录 ID 列表。

判断标准：
1. 当前任务可能重蹈这条Bug的覆辙吗？
2. 这条Bug的注意事项对当前任务有参考价值吗？
3. 这条Bug涉及的文件/函数与即将修改的内容有关联吗？

只返回相关的 ID，不相关的不要返回。
如果都不相关，返回空列表 []。

输出格式：JSON 数组，例如 ["bug_20260726_001", "bug_20260726_002"]
